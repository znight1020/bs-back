spring:
  profiles:
    active: intg

---

spring:
  config:
    activate:
      on-profile: dev
cookie:
  same-site: NONE

---

spring:
  config:
    activate:
      on-profile: prod
cookie:
  same-site: LAX
